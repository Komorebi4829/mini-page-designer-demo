import leftPanel, { dragItem } from './leftPanel'
import rightSetting, { defaultVal } from './rightSetting'
import preview from './preview'

export default { leftPanel, rightSetting, preview, dragItem, defaultVal }
